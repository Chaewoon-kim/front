1. let의 특징
-> var과 다르게 코딩 오류를 줄이기 위해 한 번 선언하고 나면 재선언이 불가하다.
-> 변수 사용 범위를 블록 내로 제한.

2. const 키워드
->  상수(변하지 않는 값)을 지정하는 키워드
-> let과 비슷하게 한 번 값을 지정하면 다시 바꿀 수 없다. 
-> 마찬가지로 변수 사용 범위를 블록 내로 제한.

3. 리터럴
-> 변수가 데이터 저장 공간의 이름이라면 리터럴은 그 공간의 값을 의미한다.
-> let a = 10; 여기서 a는 변수이고 10이 리터럴이 되는 것이다. 
